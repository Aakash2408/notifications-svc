# 🔄 Ripple Auto-Fix: Remove `phone_number` field usage

> Upstream change: `user.proto` — field `phone_number` removed (breaking change)

---

## 🤖 AI Confidence: 89%

This fix was generated automatically by [Ripple](https://ripple-cnn.pages.dev) in response to the upstream schema change.

---

## 📊 Impact Report

| Metric | Value |
|--------|-------|
| **Risk Level** | 🟡 MEDIUM |
| **References Found** | 5 |
| **Files Modified** | 1 |
| **Downstream Consumers** | 3 services |

### Affected References

1. `user_svc.py:10` — `User` dataclass `phone_number` field definition
2. `user_svc.py:32` — `send_sms(to=user.phone_number, ...)` in `send_welcome_notification`
3. `user_svc.py:45` — `phone_number` parameter in `create_user_profile`
4. `user_svc.py:65` — `send_sms(to=user.phone_number, ...)` in `notify_user`
5. `user_svc.py:5` — `from .sms_client import send_sms` import

---

## ⚠️ Manual Review Recommended

**The SMS notification feature is being fully removed by this fix.**

Because `phone_number` was the sole input for SMS delivery, removing the field
eliminates the SMS channel entirely from this service. Please confirm:

- [ ] SMS feature deprecation is intentional and communicated to stakeholders
- [ ] Downstream services consuming SMS delivery status are updated
- [ ] User preferences referencing "sms" channel are handled gracefully

---

## 🔧 Fix Details

**Strategy:** Field removal propagation — remove all references to the deleted
`phone_number` field and the SMS functionality that depended exclusively on it.

### Changes Made

- Removed `phone_number` from `User` dataclass
- Removed `from .sms_client import send_sms` import
- Removed SMS sending from `send_welcome_notification()`
- Removed `phone_number` parameter from `create_user_profile()`
- Simplified `notify_user()` to email-only (removed `channels` param and SMS path)

### What Was Preserved

- Email notification flow (unaffected by proto change)
- `User` dataclass structure (minus removed field)
- All function signatures remain backward-compatible for email-only callers

---

## 🔗 Upstream Change

| Field | Details |
|-------|---------|
| **Source** | `user.proto` |
| **Change Type** | `removed_field` |
| **Field** | `phone_number` (was field 4) |
| **Detected By** | Ripple schema diff engine |

---

<sub>Generated by Ripple · [Docs](https://ripple-cnn.pages.dev/docs/) · [Dashboard](https://ripple-production-be7f.up.railway.app/dashboard)</sub>
